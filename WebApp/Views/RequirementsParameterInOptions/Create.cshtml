@model App.BLL.DTO.RequirementParameterInOption

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>RequirementParameterInOption</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RequirementOptionId" class="control-label"></label>
                <select asp-for="RequirementOptionId" class ="form-control" asp-items="ViewBag.RequirementOptionId"></select>
            </div>
            <div class="form-group">
                <label asp-for="RequirementParameterId" class="control-label"></label>
                <select asp-for="RequirementParameterId" class ="form-control" asp-items="ViewBag.RequirementParameterId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
