@model App.Domain.EventNavigation

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>EventNavigation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PreviousEventId" class="control-label"></label>
                <select asp-for="PreviousEventId" class ="form-control" asp-items="ViewBag.PreviousEventId"></select>
            </div>
            <div class="form-group">
                <label asp-for="NextEventId" class="control-label"></label>
                <select asp-for="NextEventId" class ="form-control" asp-items="ViewBag.NextEventId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
